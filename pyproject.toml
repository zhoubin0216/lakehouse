[project]
name = "urban-data-lakehouse"
version = "0.1.0"
description = "Reusable urban data integration platform for ID2221."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
